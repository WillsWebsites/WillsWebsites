import styles from './Contact.module.css';

const Contact = props => {
    return (
        <div className={styles.contact}>
            <h4>Contact</h4>
        </div>
    );
};

export default Contact; 